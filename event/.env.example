# FoxRose Environment Configuration
# This file contains secrets and optional overrides

# Required: Telegram bot token
BOT_TOKEN=<telegram bot token>

# Optional: Custom config file path (defaults to config.yaml)
# CONFIG_PATH=/path/to/config.yaml

# Optional: Logging configuration
LOG_LEVEL=INFO
LOG_FILE=/var/log/foxrose/app.log